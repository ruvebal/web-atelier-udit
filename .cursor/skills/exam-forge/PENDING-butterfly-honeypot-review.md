# Pending review — real "butterfly/lepidoptera" honeypot field evidence

**Status: placeholder, per the operator's own instruction (2026-08-20) —
not yet reviewed, not yet analyzed. This file exists so the technique and
its real result don't get lost, not to draw conclusions from it.**

## What the operator did (their own account, condensed)

A real, already-run honeypot, of the same species as `exam-authoring.mdc`'s
essay-honeypot pattern (hidden `[ASSISTANT-INSTRUCTION: …]` span,
`metadata.honeypot_ids`) but applied differently: a hidden instruction told
an AI agent to answer **using butterfly/lepidoptera metaphors** — wings,
colors, flight, etc. Students who consulted that AI for help on a CSS/web-
styling question would produce answers explaining CSS concepts through
butterfly imagery, a signature no student would independently produce.

The operator has real student responses from this — some explain CSS/web
styling in exactly that metaphor register — and says there's more to tell
about this specific exam, and about AI-and-students findings more broadly,
when there's time to go through it properly.

## What this is not, yet

- Not reviewed: the actual response set hasn't been read or analyzed in
  this pass.
- Not generalized into a playbook rule: `anti-ai-playbook.md §2` already
  documents the honeypot *pattern*; this file is field evidence of one
  real, executed instance, kept separate until reviewed.
- Not attributed to any named exam/course/student — the operator gave the
  technique and the existence of results, not identifying specifics.

## Next step, when the operator is ready

Read the actual response set, characterize what it shows (detection rate,
any false positives, whether the metaphor-register alone was sufficient
signal or needed corroboration), and only then decide whether it becomes a
worked example in `anti-ai-playbook.md` or stays a private finding.
