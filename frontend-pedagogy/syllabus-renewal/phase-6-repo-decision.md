# Phase 6 — Repo structure decision

**Status: ✅ done 2026-08-08.** This file is a closed record, not a task brief.

## Entry

None — this was a standing open question resolved directly by Rubén, not something requiring Phase 1–5 output first (the original plan treated it as a checkpoint to revisit later; it was decided outright instead).

## Do (what was decided)

**Staying in `web-atelier-udit`** — no new repo. Rationale (from `../2026-27-syllabus-renewal-plan.md` §9): the ATELIER methodology, the evaluation rationale (`docs/evaluation/`), `AI_POLICY.md`, the `2627-fei`/`2627-feii` scaffolds, and the entire built React track already live here. Splitting FE I/FE II into a separate repo would mean re-deriving infrastructure that already works, for no stated benefit.

## Gate

- [x] Decision recorded here and in `../IGNITOR.mdc`'s cascade table.
- [x] All other phase files reference `web-foundations/docs/...` paths on the assumption this repo is the target — consistent throughout, no phase file assumes a different repo.

No further action. Referenced by the other phases; not itself re-run.
