# Unit 9 deck outline — 0.5 h

1. Vertex vs. fragment — who moves, who colours
2. GLSL is C-like: types, uniforms, varyings
3. One uniform driven from React (`uTime`)
4. Coordinate space: `uv`/`vUv` vs `gl_FragCoord` vs object space
5. Precision on mobile — `mediump` is a decision, not a default
6. Post-processing is a cost, not a default look
7. The minimum viable GLSL checklist (graded)
8. The failure mode this unit refuses: a generated look you cannot narrate
9. AI Co-Artist as the adjacent, ungrounded creation-without-literacy path
10. What Entrega 2 must show — one authored shader, explained

Speaker claims only; no unsupported learning-outcome claim.
